# DBT build commands in execution sequence for ssp_bima_import
# Run these commands in order to build the final model dependencies
dbt build --select ssp_demand_detail
dbt build --select ssp_demand_header
dbt build --select ssp_demand_detail_bima
dbt build --select ssp_demand_header_bima
